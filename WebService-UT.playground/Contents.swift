import UIKit

// Kiss principle; Keep it simple. Write simple and stright forward code.

let age = 30
let checkAge  = age > 25 ? true : false // Vioalting the KISS principle
let isTrue  = age > 25 // Keep it simple
print(checkAge)
print(isTrue)
